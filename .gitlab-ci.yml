
build:
  stage: deploy
  image: docker:stable
  services:
    - docker:dind
  script:
    - docker version
    - ls -al
    - cd implementation
    - ls -al
    - ./mvnw package -Pprod verify jib:dockerBuild


